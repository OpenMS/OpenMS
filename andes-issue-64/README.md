# Transfer directory for bigbio/andes#64 (not OpenMS content)

Session working branch only. `0001-*.patch` applies with `git am` on bigbio/andes `main`
`1254cb7`; `ISSUE_COMMENT.md` is the comment for bigbio/andes#64; `REPORT.md` and the
`arm*_eval.txt` / `*.tsv` files are the measurement. Safe to delete once transferred.
