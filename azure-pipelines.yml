# C/C++ with GCC
# Build your C/C++ project with GCC using make.
# Add steps that publish test results, save build artifacts, deploy, and more:
# https://docs.microsoft.com/vsts/pipelines/apps/c-cpp/gcc

jobs:
- job: Linux
  pool:
    vmImage: 'ubuntu-16.04'
  steps:
  script: 
  - echo hello from Linux
  - apt-get update
  - apt-get install build-essential cmake autoconf patch libtool git automake
  - apt-get install qtbase5-dev libqt5svg5-dev libboost-regex-dev libboost-iostreams-dev libboost-date-time-dev libboost-math-dev libboost-random-dev libsvm-dev libglpk-dev zlib1g-dev libxerces-c-dev seqan-dev libbz2-dev coinor-libcoinmp-dev
  - sh ./tools/quickbuild.sh
