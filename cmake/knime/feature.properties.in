# The java package name of the feature
# In maven called the artifactId
featureId=@ID@

# The human-readable name of the feature
featureName=@NAME@

# The "provider" of this feature. If not specified otherwise,
#  all subplugins will inherit this
groupId=de.openms

# The version of the feature
featureVersion=@VERSION@

# The category ID on the update site for this feature
category=@CATEGORY@
